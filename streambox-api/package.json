{
  "name": "streambox-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply streambox-db --local",
    "db:remote": "wrangler d1 migrations apply streambox-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260920.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.38.0"
  }
}
